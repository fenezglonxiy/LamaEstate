import { SelectContentDirective } from './select-content.directive';

describe('SelectContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectContentDirective();
    expect(directive).toBeTruthy();
  });
});
