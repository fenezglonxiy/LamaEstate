import { PopoverContentDirective } from './popover-content.directive';

describe('PopoverContentDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverContentDirective();
    expect(directive).toBeTruthy();
  });
});
