import { FillUpSpaceDirective } from './fill-up-space.directive';

describe('FillUpSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new FillUpSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
